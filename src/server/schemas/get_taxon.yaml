type: object
required: [id, ns]
additionalProperties: false
properties:
  id: 
    type: string
    title: Document ID
  ns:
    type: string
    title: Namespace
    enum: ['rdp_taxonomy', 'ncbi_taxonomy', 'gtdb']
  ts:
    type: integer
    minimum: 0
    description: Defaults to now
